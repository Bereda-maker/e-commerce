<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- The concurrency test needs real row-locking (SELECT ... FOR
             UPDATE) semantics across two separate connections, which
             SQLite doesn't provide the same guarantees for — so tests run
             against real MySQL (a service container in CI, or your local
             Docker MySQL) rather than an in-memory SQLite swap. Set these
             to match your local MySQL if running outside Docker. -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="ecommerce_testing"/>
    </php>
</phpunit>
